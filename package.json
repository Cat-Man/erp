{
  "name": "retail-erp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "lint": "corepack pnpm -r run lint",
    "test": "corepack pnpm -r run test",
    "test:e2e": "corepack pnpm build && playwright test tests/e2e/retail-core.spec.ts --workers=1",
    "build": "corepack pnpm -r run build",
    "dev": "corepack pnpm -r --parallel run dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
