{
  "name": "@retail-erp/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test src/**/*.test.ts test/**/*.ts",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/index.ts"
  }
}
